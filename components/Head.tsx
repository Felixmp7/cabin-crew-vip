import Head from 'next/head';

const NextHead = (): JSX.Element => (
    <Head>
        <title>Cabin Crew Vip</title>
        <link rel="icon" href="/favicon.ico" />
    </Head>
);

export default NextHead;
